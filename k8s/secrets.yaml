# [Task]: T-024
# [From]: specs/architecture.md

apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
data:
  # Base64 encoded values
  # echo -n "your-db-url" | base64
  database-url: REPLACE_WITH_BASE64_DB_URL
  # echo -n "your-secret" | base64
  better-auth-secret: REPLACE_WITH_BASE64_AUTH_SECRET
